/*
 * DataProtocol.c
 *
 *  Created on: 2017. 12. 1.
 *      Author: hw_3
 */

#include "DataProtocol.h"


char* BluetoothData(char str[BT_STR_MAX_LENGTH]){
	char* buffer_ = malloc(
			sizeof(BD)*2 + sizeof(CR) + sizeof(LF) + sizeof(SD)

			);

	return buffer_;
}
